services:
  app:
    build: .
    environment:
      APP_ENV: "production"
      DEBUG: "false"
      START_SERVER: "true"
      PORT: 3000
      API_URL: "https://api.prod.example.com"
    ports:
      - "3000:3000"
    command: ["node", "index.js"]
